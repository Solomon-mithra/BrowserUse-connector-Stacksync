{
  "metadata": {
    "workflows_module_schema_version": "1.0.0"
  },
  "fields": [
    {
      "type": "connection",
      "id": "api_connection",
      "label": "API Connection",
      "description": "Connect to your Browser Use API account.",
      "allowed_app_types": ["hyperline"],
      "allowed_connection_management_types": ["managed", "custom"]
    },
    {
      "id": "task_id",
      "type": "string",
      "label": "Task ID",
      "description": "Enter the ID of the task you want to look up."
    }
  ],
  "ui_options": {
    "ui_order": ["api_connection", "task_id"]
  }
}
